require("Base/BasePanel")
LaddersPlayerInfoPopup = Inherit(BasePanel)
local this = LaddersPlayerInfoPopup

--初始化组件（用于子类重写）
function LaddersPlayerInfoPopup:InitComponent()

 
end

--绑定事件（用于子类重写）
function LaddersPlayerInfoPopup:BindEvent()
   
end

--添加事件监听（用于子类重写）
function LaddersPlayerInfoPopup:AddListener()
 
end

--移除事件监听（用于子类重写）
function LaddersPlayerInfoPopup:RemoveListener()

end

--界面打开时调用（用于子类重写）
function LaddersPlayerInfoPopup:OnOpen()
end

--界面打开或者重新打开后，界面刷新时调用（用于子类重写）
function LaddersPlayerInfoPopup:OnShow()

    
end
function LaddersPlayerInfoPopup:OnSortingOrderChange()
end


--界面关闭时调用（用于子类重写）
function LaddersPlayerInfoPopup:OnClose()
end

--界面销毁时调用（用于子类重写）
function LaddersPlayerInfoPopup:OnDestroy()

end



return LaddersPlayerInfoPopup