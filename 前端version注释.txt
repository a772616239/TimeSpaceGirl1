packageVersion 强更版本（跟之前不相同就强更，注意好像不是的大于之前的）
version        热更版本，检测完强更后检测（跟之前不相同就热更更，注意不是的大于之前的） 
（已删除）--buglyId        腾讯buglyId（目前不知道再用没，ID先改成空字符串）
serverPathType 服务器类型
channel        不知道干啥用（目前好像没用用到，先不管）
subChannel     好像是当前子渠道（但是不应该在这里设置，先不管）
sdkLodingUrl   SDK登录地址
payUrl         支付地址
serverUrl      游戏服务器地址
resUrl         热更资源地址
logUrl         Log上传地址